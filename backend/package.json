{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "type": "module",
  "scripts": {
   "server" : "nodemon server.js",
   "prod_run": "node server.js",
   "frontend_build": "cd ../frontend && npm install && npm run build",
   "admin_build": "cd ../admin && npm install && npm run build",
   "backend_build": "npm install",
   "build_prod": "npm run frontend_build && npm run admin_build && npm run backend_build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.4.1",
    "multer": "^1.4.5-lts.1",
    "nodemon": "^3.1.2",
    "stripe": "^15.8.0",
    "validator": "^13.12.0"
  }
}
